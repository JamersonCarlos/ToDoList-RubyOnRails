
<!-- Header Section -->
<div class="title_section_tasks">
  <h2>🛎️ Incomplete tasks - <%= filter_not_completed(@tasks).length %></h2>
</div>
<!-- Chosen filter -->
<%= render 'tasks/dropdown_filter', select: 'Incompleted Tasks' %>
<!-- View incompleted Tasks -->
<div class="list_tasks">
  <%= render 'tasks/list_tasks', tasks: filter_not_completed(@tasks) %>
</div>