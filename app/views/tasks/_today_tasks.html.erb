<!-- Header Section -->
<div class="title_section_tasks">
  <h2>🕰️ Tasks for today - <%= filter_tasks_today(@tasks).length %></h2>
</div>
<!-- Chosen filter -->
<%= render 'tasks/dropdown_filter', select: 'Tasks for today' %>
<!-- View today tasks-->
<div class="list_tasks">
  <%= render 'tasks/list_tasks', tasks: filter_tasks_today(@tasks) %>
</div>